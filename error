test_sound.play()
win.callOnFlip(trialClock.reset)  # Start clock on flip

# Prepare triggers
trigger_times_sec = [
    float(trigger_item) / 1000.0,
    float(trigger_senonset) / 1000.0,
    float(trigger_verbsonset) / 1000.0,
    float(trigger_1_F0) / 1000.0,
    float(trigger_suffix) / 1000.0,
    float(trigger_2_F0) / 1000.0,
]
trigger_codes = [
    int(ID_item),
    int(ID_senonset),
    int(ID_verbonset),
    int(ID_1_F0),
    int(ID_suffix),
    int(ID_2_F0),
]
next_trigger_index = 0
