for delay, code in zip(trigger_times_sec, trigger_codes):
    wait_time = delay - trialClock.getTime()
    if wait_time > 0:
        core.wait(wait_time)
    send_trigger(code)
    print(f"Trigger {code} sent at {trialClock.getTime():.6f} sec")
