t0 = test_sound.play()
if t0 is not None:
    print(f"[DEBUG] test_sound.play() called — estimated audio onset: {t0 * 1000:.2f} ms")
else:
    print("[DEBUG] test_sound.play() called — no PTB onset time returned.")
