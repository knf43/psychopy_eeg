# Get the loop that called this routine
current_loop = currentLoop  # Get the most recent loop

# Only run this routine after the last trial in the loop
if current_loop.thisN != len(current_loop.trialList) - 1:
    continueRoutine = False
