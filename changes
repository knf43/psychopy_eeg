# Play the sound and get the best estimate of audio onset (in seconds)
t0 = test_sound.play()

# Create and reset the trial clock
trialClock = core.Clock()
trialClock.reset()

print(f"[DEBUG] test_sound.play() called — estimated audio onset: {t0*1000:.2f} ms")
